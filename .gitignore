.DS_Store

# Skill content is per-machine — installed by `npx skills` from .skill-lock.json.
# Only the lock file is tracked.
.agents/skills/

# Never commit secrets
.env
.env.*
*.key
*.pem
*.p12
*.pfx
id_*
!id_*.pub
.netrc
*credentials*
*.token
.claude/secrets.env
